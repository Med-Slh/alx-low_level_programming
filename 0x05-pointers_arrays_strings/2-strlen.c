#include "main.h"
/**
 * _strlen - return the length of a string
 *
 * @s: charachter
 * Return: the length of a string
 */

int _strlen(char *s)
{
	int count = 0;

	while (*s != '\0')
	{
		count++;
		p++;
	}

	return (count);
}
